const mongoose = require('mongoose'); require('dotenv').config(); const CurrentAffair = require('./models/CurrentAffair'); async function deleteOld() { try { await mongoose.connect(process.env.MONGO_URI); const dateThreshold = new Date('2026-06-29T00:00:00Z'); await CurrentAffair.deleteMany({ date: { $lt: dateThreshold } }); console.log('Old records deleted.'); process.exit(0); } catch(err) { console.error(err); process.exit(1); } } deleteOld();
